#quick sort algorithm 

def quicksort(arr):
    if len(arr) <= 1:
        return arr
    stack = []
    stack.append((0, len(arr)-1))
    while len(stack) > 0:
        pass
def main():
    arr = [50,30,80,20,10,70]
    empty_array = []
    quicksort(empty_array)
main()
